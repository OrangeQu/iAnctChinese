spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ianct_chinese?useSSL=false&useUnicode=true&characterEncoding=utf8&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  # --- 新增配置：允许启动时运行 data.sql 插入数据 ---
  sql:
    init:
      mode: always
      encoding: UTF-8
  # -----------------------------------------------

  jpa:
    # --- 新增配置：延迟数据初始化，确保表创建后再插入数据 ---
    defer-datasource-initialization: true
    # -----------------------------------------------
    
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  
  jackson:
    default-property-inclusion: non_null

server:
  port: 8080

# logging:
#   level:
#     org.springframework.web: INFO
#     com.ianctchinese: DEBUG

logging:
  file:
    name: target/app.log
  level:
    root: INFO
    com.ianctchinese: DEBUG
    org.springframework.web: INFO

# backend/src/main/resources/application.yml

siliconflow:
  api-key: sk-rozyqiofobetxzpozmiqfxnwemhvpxvotndkbugnrkaurcwj
  default-model: deepseek-ai/DeepSeek-V3



tmap:
  key: J5ABZ-GNNRN-4BHFW-SGFPX-IJ4YS-JCBV3
  sk: VMjXTReGHohVTcVy54Jc09dND65H6pQA
